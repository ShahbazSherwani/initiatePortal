# Make.com Bidirectional Sync - Architecture Diagram

## System Flow Diagram

```
┌──────────────────────────────────────────────────────────────────────────┐
│                         BIDIRECTIONAL USER SYNC                           │
│                         via Make.com Automation                           │
└──────────────────────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════════════
SCENARIO 1: InitiatePH → InitiateGlobal
═══════════════════════════════════════════════════════════════════════════

┌─────────────────┐
│  User Registers │
│  on InitiatePH  │
│ (React Frontend)│
└────────┬────────┘
         │
         │ 1. createUserWithEmailAndPassword()
         ▼
┌─────────────────┐
│ Firebase Auth   │
│ Creates User    │
└────────┬────────┘
         │
         │ 2. upsertProfile(token, fullName)
         ▼
┌─────────────────────────────┐
│  InitiatePH Backend         │
│  POST /profile              │
│  - Saves to Supabase        │
│  - Splits name              │
│  - notifyMakeOfNewUser() ◄──┼─── 🔔 NEW: Calls Make webhook
└─────────────┬───────────────┘
              │
              │ 3. Webhook POST
              │ {source_system: "PH", user: {...}}
              ▼
┌──────────────────────────────┐
│    Make.com Scenario 1        │
│  "PH to Global User Sync"     │
│                               │
│  [Webhook Trigger]            │
│          ↓                    │
│  [Router: source=PH?] ───┐   │
│          ↓                │   │
│  [HTTP: Check WP User]    │   │
│          ↓                │   │
│  [Router: Exists?]        │   │
│      ↙         ↘          │   │
│  [Create]   [Update]      │   │
│          ↓                │   │
│  [HTTP: POST WP API]      │   │
└───────────┬──────────────┘   │
            │                   │
            │ 4. WordPress API  │
            ▼                   │
┌─────────────────────────┐    │
│  InitiateGlobal WP      │    │
│  - Create wp_users      │    │
│  - Create wp_jet_cct    │    │
│  - Set meta:            │    │
│    synced_from = "PH" ──┼────┘ Loop protection!
│    ph_user_id = 123     │
└─────────────────────────┘

✅ Result: User on PH automatically exists on Global


═══════════════════════════════════════════════════════════════════════════
SCENARIO 2: InitiateGlobal → InitiatePH
═══════════════════════════════════════════════════════════════════════════

┌─────────────────────────┐
│  User Registers on      │
│  InitiateGlobal         │
│  (WordPress)            │
└────────┬────────────────┘
         │
         │ 1. WordPress creates user
         ▼
┌─────────────────────────────┐
│  WordPress Action           │
│  user_register              │
└────────┬────────────────────┘
         │
         │ 2. Custom Hook
         ▼
┌────────────────────────────────────┐
│  WordPress Snippet                  │
│  initiate_sync_user_to_make()       │
│  - Check synced_from meta ◄─────┐  │ Loop protection!
│  - If from PH → STOP            │  │
│  - Else → Call Make webhook     │  │
└─────────────┬──────────────────┘  │
              │                      │
              │ 3. Webhook POST      │
              │ {source_system: "GLOBAL", user: {...}}
              ▼                      │
┌──────────────────────────────┐    │
│    Make.com Scenario 2        │    │
│  "Global to PH User Sync"     │    │
│                               │    │
│  [Webhook Trigger]            │    │
│          ↓                    │    │
│  [Router: source=GLOBAL?] ─┐ │    │
│          ↓                  │ │    │
│  [HTTP: Check PH User]      │ │    │
│          ↓                  │ │    │
│  [Router: Exists?]          │ │    │
│      ↙         ↘            │ │    │
│  [Create]   [Update]        │ │    │
│          ↓                  │ │    │
│  [HTTP: POST /api/sync-user]│ │    │
└───────────┬─────────────────┘ │    │
            │                   │    │
            │ 4. InitiatePH API │    │
            ▼                   │    │
┌─────────────────────────┐    │    │
│  InitiatePH Backend     │    │    │
│  POST /api/sync-user    │    │    │
│  - Check source_system ─┼────┴────┘ Loop protection!
│  - If PH → STOP         │
│  - Create Firebase user │
│  - Save to Supabase     │
│  - Send password reset  │
└─────────────────────────┘

✅ Result: User on Global automatically exists on PH


═══════════════════════════════════════════════════════════════════════════
LOOP PROTECTION MECHANISM
═══════════════════════════════════════════════════════════════════════════

Prevents infinite sync loops using TWO safeguards:

1. Source System Check (in Make.com):
   ┌─────────────────────────────────────────┐
   │ Router Filter                            │
   │ IF source_system == "PH"  → Continue    │
   │ IF source_system == "GLOBAL" → STOP     │
   └─────────────────────────────────────────┘

2. Metadata Check (in destination system):
   
   WordPress:
   ┌─────────────────────────────────────────┐
   │ IF get_user_meta('synced_from') == 'PH' │
   │    THEN don't call Make webhook         │
   └─────────────────────────────────────────┘
   
   InitiatePH:
   ┌─────────────────────────────────────────┐
   │ IF source_system == 'PH'                │
   │    THEN return "Loop protection"        │
   └─────────────────────────────────────────┘


═══════════════════════════════════════════════════════════════════════════
DATA FLOW EXAMPLE: User Registers on InitiatePH
═══════════════════════════════════════════════════════════════════════════

Step 1: User fills registration form
  ┌──────────────────────────┐
  │ Email: john@example.com   │
  │ Name: John Doe           │
  │ Password: ********       │
  └──────────────────────────┘

Step 2: InitiatePH Backend processes
  ┌──────────────────────────────────┐
  │ Supabase users table:             │
  │ - firebase_uid: abc123            │
  │ - email: john@example.com         │
  │ - first_name: John                │
  │ - last_name: Doe                  │
  │ - full_name: John Doe             │
  │ - phone_number: +1234567890       │
  │ - role: borrower                  │
  └──────────────────────────────────┘

Step 3: Make.com receives webhook
  ┌──────────────────────────────────┐
  │ Payload:                          │
  │ {                                 │
  │   source_system: "PH",            │
  │   source_event_id: "uuid-123",    │
  │   user: {                         │
  │     email: "john@example.com",    │
  │     first_name: "John",           │
  │     last_name: "Doe",             │
  │     firebase_uid: "abc123",       │
  │     ph_user_id: 456               │
  │   }                               │
  │ }                                 │
  └──────────────────────────────────┘

Step 4: Make creates WordPress user
  ┌──────────────────────────────────┐
  │ WordPress wp_users:               │
  │ - user_login: john@example.com    │
  │ - user_email: john@example.com    │
  │ - first_name: John                │
  │ - last_name: Doe                  │
  │                                   │
  │ WordPress wp_usermeta:            │
  │ - firebase_uid: abc123            │
  │ - ph_user_id: 456                 │
  │ - synced_from: PH ◄── Loop blocker│
  │                                   │
  │ WordPress wp_jet_cct_users:       │
  │ - email: john@example.com         │
  │ - first_name: John                │
  │ - last_name: Doe                  │
  └──────────────────────────────────┘

Step 5: WordPress fires user_register hook
  ┌──────────────────────────────────┐
  │ WordPress Snippet checks:         │
  │ IF synced_from == "PH":           │
  │   → STOP (Loop protection)        │
  │ ELSE:                             │
  │   → Call Make webhook             │
  │                                   │
  │ ✅ Stopped! No infinite loop      │
  └──────────────────────────────────┘


═══════════════════════════════════════════════════════════════════════════
SECURITY LAYERS
═══════════════════════════════════════════════════════════════════════════

Layer 1: API Key Authentication
  ┌─────────────────────────────────┐
  │ Make → InitiatePH:               │
  │ Header: X-API-Key: <secret>      │
  │                                  │
  │ Verified in verifyMakeRequest()  │
  └─────────────────────────────────┘

Layer 2: WordPress Application Password
  ┌─────────────────────────────────┐
  │ Make → WordPress:                │
  │ Authorization: Basic <base64>    │
  │                                  │
  │ WordPress validates credentials  │
  └─────────────────────────────────┘

Layer 3: Event ID Deduplication (optional)
  ┌─────────────────────────────────┐
  │ source_event_id: unique UUID     │
  │ Can be logged to prevent replays │
  └─────────────────────────────────┘

Layer 4: Password Security
  ┌─────────────────────────────────┐
  │ ❌ Passwords are NEVER synced    │
  │ ✅ Users created with temp pass  │
  │ ✅ Password reset email sent     │
  └─────────────────────────────────┘


═══════════════════════════════════════════════════════════════════════════
MONITORING & DEBUGGING
═══════════════════════════════════════════════════════════════════════════

Make.com Dashboard:
  ┌─────────────────────────────────┐
  │ Scenarios → History              │
  │ - View all executions            │
  │ - See success/error rates        │
  │ - Inspect payloads               │
  │ - Check execution time           │
  └─────────────────────────────────┘

WordPress Logs:
  ┌─────────────────────────────────┐
  │ /wp-content/debug.log            │
  │ - Webhook calls logged           │
  │ - Loop protection messages       │
  │ - Error details                  │
  └─────────────────────────────────┘

InitiatePH Logs:
  ┌─────────────────────────────────┐
  │ Render console logs              │
  │ - Make webhook calls             │
  │ - Sync user operations           │
  │ - Database queries               │
  └─────────────────────────────────┘


═══════════════════════════════════════════════════════════════════════════
COSTS & SCALABILITY
═══════════════════════════════════════════════════════════════════════════

Make.com Operations per User Sync:
  1. Webhook received ................... 1 op
  2. Router check ...................... 1 op
  3. HTTP check if user exists ......... 1 op
  4. HTTP create/update user ........... 1 op
  ─────────────────────────────────────────
  Total per sync: 4 operations

Free Tier Capacity:
  1,000 operations/month ÷ 4 = 250 user syncs/month

Paid Tier ($9/mo):
  10,000 operations/month ÷ 4 = 2,500 user syncs/month

Optimization:
  - Cache user existence checks
  - Batch updates (if many at once)
  - Use data stores for deduplication


═══════════════════════════════════════════════════════════════════════════
FUTURE ENHANCEMENTS
═══════════════════════════════════════════════════════════════════════════

✅ Currently Implemented:
  - User registration sync (both directions)
  - Loop protection
  - Error handling
  - Secure authentication

🔮 Future Additions:
  - Profile update sync (name, phone changes)
  - Role/permission sync
  - Account deletion sync
  - Avatar/profile picture sync
  - Email verification status sync
  - Real-time updates (webhooks for all changes)
  - Conflict resolution (which system wins)
